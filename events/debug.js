// THIS EVENT IS FOR DISCORD-PLAYER.JS!!! Neede in case shit breaks in the future
// You can ignore the log message: "Invalid event file: debug.js missing: 'client.on' or 'client.once'"
/*
const { GuildQueueEvent } = require("discord-player");

module.exports = (client) => {
  const logPrefix = "[Debug]: ";

  client.player.on(GuildQueueEvent.Debug, async (message) => {
    console.log(logPrefix + message);
  });
};
*/
